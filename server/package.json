{
  "devDependencies": {
    "express": "^4.17.1",
    "mongodb": "^3.5.0",
    "nodemon": "^2.0.2"
  },
  "scripts": {
    "start mongo": "docker container rm -f mongo-cats-mash;docker container run -p 27018:27017 -d -v dataMongo:/data/db -e MONGO_INITDB_ROOT_USERNAME=melcaro -e MONGO_INITDB_ROOT_PASSWORD=1234 --name mongo-cats-mash mongo:latest",
    "initDB": "node ./scripts/init.js",
    "start:dev": "nodemon index.js"
  }
}
